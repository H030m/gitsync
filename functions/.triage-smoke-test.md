# PR triage smoke test

This file exists only to drive a no-op pull request for verifying the
`onPullRequestOpened` triage agent end-to-end on the live deployment:

- Confirms the webhook → `pullRequests/{n}` write → trigger → Discord post chain.
- Path starts with `functions/`, so the `touches-functions` risk tag should fire.
- Tiny diff, so `large-diff` should NOT fire.

This file is safe to delete (or just close the PR without merging).
