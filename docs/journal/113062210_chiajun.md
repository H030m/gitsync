# 倪嘉駿 (113062210) 工作日誌

> 最新日期在**最上面**。每次寫程式完都要在這裡寫一則。格式參考 [`AI_AGENT_RULES.md §2.2`](../AI_AGENT_RULES.md#22-每篇日誌必填項目)。
>
> 寫完一則後**記得**也更新 [`_index.md`](./_index.md)。

---

## 2026-05-27 — Fake backend 模式：UI 不靠 Firebase / API 就能跑

**做了什麼**

- 加 [`lib/config/app_config.dart`](../../lib/config/app_config.dart)：`--dart-define=BACKEND=fake|live` 切換；`AppConfig.defaultBackend = Backend.fake`。
- 加 [`lib/data/dummy_data.dart`](../../lib/data/dummy_data.dart)：3 users + 1 repo + 8 tasks + 3 commits + 1 PR + 3 Discord msgs + 1 daily report，提供 fake mode 的所有 seed。
- 把 8 個 Repository 全部重構成 `abstract class XxxRepository` + 私有 `_LiveXxxRepository` (Firestore) + 公開 `FakeXxxRepository` (in-memory，在 `lib/repositories/fake/` 下)。Live 不動原本 Firestore 邏輯；Fake 用 [`_replay_state.dart`](../../lib/repositories/fake/_replay_state.dart) 提供 replay 行為（新 subscriber 也能看到最新值）。
- 把 `AuthenticationService` / `FunctionsService` 重構成同一個 pattern。`FakeAuthenticationService` 自動以 `DummyData.demoUserId` 簽入；`FakeFunctionsService` 對 `breakdownTask` 回 4 個 generic subtask、`generateHandoff` 回一段假 markdown，等等。
- [`lib/main.dart`](../../lib/main.dart)：fake mode skip `Firebase.initializeApp`。
- [`lib/router/app_router.dart`](../../lib/router/app_router.dart) 加 redirect：fake mode + autoSignInDemoUser 時 `/` 自動跳 `/repos`，省掉 SignInPage。
- [`SettingsPage`](../../lib/views/settings/settings_page.dart) 頂端加 `_BackendBanner`，明顯標示「FAKE / LIVE」+ 切換指令。
- 三個小修：
  - **gitignore**: 修掉 line 59 的 inline comment 寫法（gitignore 不認 inline `#`），`git rm --cached lib/firebase_options.dart android/app/google-services.json` 把已被舊 commit track 進去的兩個檔從 index 移除（檔案還在 disk，下次 commit 起 git 就不再追蹤）。新增 `lib/firebase_options.example.dart` placeholder（committed），fresh clone 後 `Copy-Item` 一下就能編譯。
  - **firestore.indexes.json**: 之前被 `firebase init firestore` 清空，補回 3 個 index（tasks dependsOn array-contains、commits vector、discordMessages vector）。
  - **region**: 全 codebase + docs 從 `us-west1` 改成 `asia-east1`（[`MEMORY.md` 對應決策](../MEMORY.md#2026-05-27--cloud-functions-region-改成-asia-east1取代-us-west1)）。動了 `functions/src/admin.ts`、`lib/services/functions_service.dart`、`docs/ARCHITECTURE.md` §4.1 + Cloud Tasks 指令、`docs/COURSE_METHODS.md` 6 處範例、`docs/AI_AGENT_RULES.md` §0/§3.6/§4.3 + 章節日期、`docs/MEMORY.md`、`functions/README.md`、`functions/src/triggers/scheduledDailyReport.ts` 註解。

**為何這樣做**

- 五人並行開發時最大的痛是「卡別人 setup」。fake mode 讓 UI / VM 工作不依賴 Firebase + OpenAI + GitHub OAuth provider 三件事。隊員想實作 TasksBoardPage 拖拉、想試 breakdownTask UI flow，clone 之後 30 秒內就能 `flutter run` 看到自己改的 UI 跑起來。
- 採 `abstract + Live + Fake` 而不是 monkey-patch 是因為 Live 那層仍要照 ARCHITECTURE §2.2 Rules 跟真實 Firestore 對接，混雜兩種模式在同 class 內會變垃圾。每個 Repository 保留純的 live 實作 + 純的 fake 實作，未來 unit test 也可以直接 inject Fake。
- 用戶在 secret/firebase 還沒 setup 好時提到的「先用 debug 模式跳過」——fake mode 對 OpenAI/Discord secret 不依賴（`.secret.local` 不需填），GitHub OAuth provider 沒啟用也沒差。同時保留 TODO 在 `authentication.dart` / `functions_service.dart` 註明 D / E 模組接手後該做什麼。

**做不下去的地方 / 給下一個人**

- **fresh clone 後必須 `Copy-Item lib/firebase_options.example.dart lib/firebase_options.dart`** 一次，否則 main.dart 編譯不過（它 `import 'firebase_options.dart'`）。文件已記在 [`MEMORY.md`](../MEMORY.md#2026-05-27--fake-backend-模式appconfigusefakebackend) 與 [`secrets/README.md`](../../secrets/README.md)。
- **Fake mode 的 dummy data 不持久化** — 跨次 `flutter run` 之間 reset。要持久化需加 SharedPreferences 之類，但 fake mode 的設計初衷是「dev 跑 UI」不是「demo 給老師」，不做。
- **`FakeFunctionsService.breakdownTask` 不管你輸入什麼 goal**，固定回那 4 個 generic subtask。AI flow 真正實作好之後（D 模組）就會看到合理的 LLM 拆解。
- **Android `google-services.json` 仍 gitignored**。fresh clone 的隊員想跑 Android build 必須先跑 `flutterfire configure` 或從別人那裡複製一份。Fake mode 跑在 Chrome / Windows / iOS Simulator 都不依賴此檔。

**動了哪些檔案**

新建（24 個）：
- `lib/config/app_config.dart`
- `lib/data/dummy_data.dart`
- `lib/firebase_options.example.dart`
- `lib/repositories/fake/_replay_state.dart`
- `lib/repositories/fake/fake_user_repo.dart`
- `lib/repositories/fake/fake_repo_repo.dart`
- `lib/repositories/fake/fake_task_repo.dart`
- `lib/repositories/fake/fake_commit_repo.dart`
- `lib/repositories/fake/fake_pull_request_repo.dart`
- `lib/repositories/fake/fake_discord_message_repo.dart`
- `lib/repositories/fake/fake_daily_report_repo.dart`
- `lib/repositories/fake/fake_member_repo.dart`
- `lib/services/fake/fake_authentication.dart`
- `lib/services/fake/fake_functions_service.dart`

修改（重大）：
- `lib/repositories/{user,repo,task,commit,pull_request,discord_message,daily_report,member}_repo.dart` (8) — 全部變成 abstract + Live
- `lib/services/{authentication,functions_service}.dart` (2)
- `lib/main.dart` — fake-mode skip Firebase init
- `lib/router/app_router.dart` — redirect signed-in 跳過 SignInPage
- `lib/views/settings/settings_page.dart` — `_BackendBanner`
- `lib/services/functions_service.dart` + `functions/src/admin.ts` — region 改 asia-east1
- `firestore.indexes.json` — 補回 3 個 index
- `.gitignore` — 修掉 line 59、新增 plist / google-services 排除

修改（文件）：
- `docs/ARCHITECTURE.md` §4.1 + Cloud Tasks 指令
- `docs/AI_AGENT_RULES.md` §0/§3.6/§4.3 region 條目
- `docs/COURSE_METHODS.md` 範例的 region
- `docs/MEMORY.md` — 兩條新決策
- `functions/README.md` — region
- `functions/src/triggers/scheduledDailyReport.ts` — 註解

`git rm --cached`（檔案還在 disk）：
- `lib/firebase_options.dart`、`android/app/google-services.json`

**驗證了什麼 / 還沒驗證**

- ✅ `flutter analyze` — **0 error / 0 warning**（剩 1 個 info-level `use_null_aware_elements`，不擋）
- ❌ `flutter run --dart-define=BACKEND=fake` 還沒實際在裝置上跑（要等下次 `flutter run` 確認，但 analyze 過代表程式碼沒語法 / 型別錯）
- ❌ `flutter run --dart-define=BACKEND=live` 暫時會 throw `HttpsError unimplemented`（因為 `functions/src/flows/*.ts` 都還是 stub），這是預期內，等 D 模組補完

💬 **建議 commit message**（建議拆 2 個 commit，依 [AI_AGENT_RULES.md §4.6.1](../AI_AGENT_RULES.md#461-commit-message-格式守則)）：
```
1. Add fake backend mode for offline / no-API dev

   Refactors every Repository + AuthenticationService + FunctionsService
   into an abstract interface with private LiveX (Firestore-backed) and
   public FakeX (in-memory) impls. AppConfig.useFakeBackend picks at
   construction time. main.dart skips Firebase.initializeApp in fake
   mode; router auto-redirects past SignInPage. Includes seeded
   DummyData (users / repos / tasks / commits / discord / daily report)
   and a backend-mode banner in SettingsPage.

2. Switch Cloud Functions region to asia-east1; fix gitignore for Firebase configs

   Replaces us-west1 with asia-east1 across admin.ts, functions_service.dart,
   ARCHITECTURE.md, AI_AGENT_RULES.md, COURSE_METHODS.md, MEMORY.md, and
   triggers. Fixes a malformed inline-comment line in .gitignore that left
   firebase_options.dart unfilterable; untracks the file via git rm --cached
   and adds firebase_options.example.dart as the post-clone placeholder.
   Restores firestore.indexes.json indexes that firebase init had cleared.
```

---

## 2026-05-26 17:20 — AI_AGENT_RULES §4.6 收尾回報加「💬 建議 commit message」欄

**做了什麼**
- [`AI_AGENT_RULES.md §4.6`](../AI_AGENT_RULES.md#46-給使用者的回報) 從 ✅📁⚠️🧪 四欄改成 **✅📁⚠️🧪💬 五欄**，新增 §4.6.1 commit message 格式守則（英文、imperative、subject ≤72 字、跨範圍時拆多條、AI 只生成字串）。
- §8 TL;DR 第 6 條同步更新。
- MEMORY.md 加一條記錄此變動。

**為何這樣做**
- 隊員 review PR 時看 commit log 比看 journal 快；AI 反正有完整脈絡，順手產一行訊息成本 0。
- 仍受 [§R1](../AI_AGENT_RULES.md#r1-ai-不可自己-commit--push--任何寫-git-歷史的動作) 限制——AI **不執行** `git commit`，只生成字串給使用者複製。

**做不下去的地方 / 給下一個人**
- 既有 commit history（`Initial project` / `Architecture Update...`）就保持原樣，不溯及既往。

**動了哪些檔案**
- `docs/AI_AGENT_RULES.md` (mod, §4.6 + 新增 §4.6.1 + §8 第 6 條)
- `docs/MEMORY.md` (mod, +1 條決策)

**驗證了什麼 / 還沒驗證**
- N/A — 純文件變更

---

## 2026-05-26 16:50 — Sprint 1 骨架：lib/ MVVM 全層 + functions/ TS 全模組 + secrets/ 中央倉

**做了什麼**

- **Flutter 端 `lib/` 五層 MVVM 全鋪**：依 [`ARCHITECTURE.md §3`](../ARCHITECTURE.md#3-前端-flutter--頁面對應) + [`COURSE_METHODS.md §0`](../COURSE_METHODS.md#0-專案目錄結構mvvm-分層必須遵守) 完整建：`theme/`、`models/`、`repositories/`、`services/`、`view_models/`、`router/`、`views/`、`main.dart`、`firebase_options.dart` (placeholder)。9 個 Model 含完整 `fromMap`/`toMap`；8 個 Repository 含 stream / CRUD wiring（讀寫權限照 Firestore Rules 切割：commits / PRs / discordMessages / dailyReports / members 都標明 read-only）；8 個 ViewModel 含 stream subscription 與 `dispose()` cancel；所有 Page 是可跑的 Scaffold stub，已連 Provider／NavigationService／FunctionsService。
- **Cloud Functions `functions/` TypeScript 專案**：建 `package.json` (Node 22 + firebase-functions v2 + openai + zod + @octokit/rest)、`tsconfig.json` (strict)、`src/admin.ts` (singleton init)、`src/config.ts` (defineSecret + getOpenAI)、`src/types.ts` (zod schemas)。**12 個 handler stub**：addRepo / removeRepo / breakdownTask（含分散式鎖實作！）/ forceUnlockBreakdown / assignTask / generateHandoff / summarizeDay / setDiscordWebhook / subscribeToTopic / githubWebhook / discordMessageIngest / dailyReportWorker。**7 個 trigger stub**：onTaskCreated / onTaskUpdated / onCommitCreated / onPRMerged / onDiscordMessageCreated / scheduledDailyReport / scheduledUnstickBreakdown（後兩個排程含 cron schedule + 分散式鎖兜底解鎖實作）。**4 個 flow stub**：breakdownTask（含 `detectCycles` DFS 工具）/ assignTask / generateHandoff / summarizeDay。**5 個 tool**：idempotency / commitFilter (regex 黑名單已實作) / discordFilter (噪聲 regex 已實作) / discordNotify (fetch + timeout 已實作) / embedding。**1 個 service**：githubClient (getOctokit + getRecentCommits 已實作)。**4 個 prompt 檔**：純字串 system prompt。
- **Secrets 中央倉**：依使用者「自己放一個資料夾」的需求，建 `secrets/` 根目錄 + `secrets/README.md`（教大家如何 setup + 漏 key 應急流程）+ `*.env.example` 範本（openai / discord / github）。同時建 `functions/.secret.local.example` 作為 Firebase emulator 慣例。`.gitignore` 整個 `secrets/*` 排除但保留 `*.example` 與 `README.md`，並排除 `firebase_options.dart`、`functions/.secret.local`、`/functions/node_modules/` 等。
- **Firestore config**：`firestore.rules` 完整抄自 ARCHITECTURE §2.2（5 個 read-only collection 設 `allow write: if false`）、`firestore.indexes.json` 含 tasks.dependsOn 複合索引 + commits/discordMessages 兩個 vector index、`firebase.json` 設 emulator port + functions predeploy build。
- **dep 安裝**：`flutter pub get` 順利、`npm install` 順利、`flutter analyze` 通過、`tsc --noEmit` 通過。

**為何這樣做**

- 五人協作的硬需求是「接口先固定」。我把 Model 的 `fromMap`/`toMap`、Repository 的 stream/CRUD method 簽章、ViewModel 的 ChangeNotifier 結構、Functions 的 callable input/output、Firestore 路徑常數通通寫死成「合約」。隊員只要往對應檔案內補實作（多半是 `throw new Error('not implemented yet')` 換成真內容），不會碰到設計爭議。
- 課程 lint `prefer_initializing_formals` 跟 [課程 model pattern](../COURSE_METHODS.md#41-model--null-safe--firestore-timestamp)（`_createdAt` 私有欄位 + getter fallback）衝突，所以在 `analysis_options.yaml` 統一關掉這個 rule（決策也寫進 MEMORY）。
- `breakdownTask` handler 的**分散式鎖直接寫好**而非留 stub，因為 [`ARCHITECTURE.md §5.1`](../ARCHITECTURE.md#51-flow-1--breakdowntaskflow任務拆解) 強調這是「兩個成員同時點會直接爛掉」的關鍵防護，不能留給隊員「之後再說」。`scheduledUnstickBreakdown` 兜底排程也完整寫好。
- secrets 採「root `secrets/` + `functions/.secret.local` 兩層」是因為：root 那層人類 ↔ AI 看得到，是「中央事實來源」；`functions/.secret.local` 是 Firebase emulator 慣例（部署不用，emulator 才用），兩邊都列在 `.gitignore`。
- `firebase_options.dart` 故意留成 throws UnsupportedError 的 placeholder——讓 `flutter analyze` 過但 runtime 會在啟動時提示「請跑 `flutterfire configure`」。實際接 Firebase 專案後 flutterfire CLI 會自動覆寫。
- TODO 在 view 跟 flow 大量留下，但**每個 TODO 都點名對應的 Sprint 與 ARCHITECTURE 章節**，例如 `// TODO Sprint 4: parse #N from commit message, compute embedding...`，讓接手的人不用回頭翻文件。

**做不下去的地方 / 給下一個人**

- **`flutterfire configure` 必須由「人類」親跑**（[AI_AGENT_RULES §R2](../AI_AGENT_RULES.md#r2-ai-不可自己-deploy-或改線上資源)）→ 在還沒跑前，flutter build / run 都會在啟動時 throw `UnsupportedError`。`flutter analyze` 不受影響。
- **`firebase functions:secrets:set OPENAI_API_KEY` / `DISCORD_INGEST_SECRET` 必須由人親跑**。本機 emulator 可改在 `functions/.secret.local` 填入。
- **Cloud Tasks queue 沒建**：`scheduledDailyReport` 預期把每個 repo 投到 `daily-report-queue`，但 queue 必須人親跑 `gcloud tasks queues create daily-report-queue --location=us-west1`，且 Cloud Tasks Node SDK 也還沒裝（我故意沒裝，Sprint 4 真的要扇出時再決定要不要 `@google-cloud/tasks`）。
- **Discord forwarder bot 完全沒寫**：那是 `functions/` repo 外的東西（[ARCHITECTURE.md §7.2](../ARCHITECTURE.md#72-inbound--訊息怎麼進-firestore) Option A 寫得很清楚），由跑 forwarder 的人自己用 discord.js 寫，需重用 `functions/src/tools/discordFilter.ts` 的規則（兩邊規則必須同步）。
- **lib/widgets/ 是空的**：沒寫 reusable component（TaskCard、CommitTile 等），各 view 直接 inline ListTile。Sprint 2/3 視需求拉出來重構。
- **flow 全部是 `throw new Error('not implemented yet')`**：handler 已經串好 auth check + lock + invocation，但 OpenAI 真正的呼叫邏輯沒做。Sprint 2 的 D 模組（AI Agent）開工時直接補 `flows/breakdownTask.ts` 那一支即可，其他三支照葫蘆畫瓢。
- **`use_null_aware_elements` info-level lint 還有一個**（`user_repo.dart:47`）。Dart 3.5+ 的 `'githubAccessToken': ?githubAccessToken,` 語法可消，但不擋 §4.1 檢查所以先擱著。
- **`assignTaskFlow.readTeamState` / `generateHandoffFlow.readTeamRoster` 必須回傳三組身份對照**（userId / githubLogin / discordUserId），詳見 [MEMORY.md 2026-05-26 "users 必加 discordUserId 對照欄位"](../MEMORY.md)。我已經在 prompt 內寫死「use readTeamRoster to map IDs」，做 flow 的人實作 tool 時不要漏。

**動了哪些檔案**

新建（大致清單，總共 ~50 個檔）：
- `lib/firebase_options.dart`（placeholder）、`lib/main.dart`（mod，原 Demo counter 砍掉）
- `lib/theme/{app_colors.dart,app_theme.dart}`
- `lib/models/{app_user,repo,task,sub_task,commit,pull_request,discord_message,daily_report,member}.dart` (9)
- `lib/repositories/{firestore_paths,user_repo,repo_repo,task_repo,commit_repo,pull_request_repo,discord_message_repo,daily_report_repo,member_repo}.dart` (9)
- `lib/services/{authentication,navigation,push_messaging,functions_service,theme_mode_notifier}.dart` (5)
- `lib/view_models/{auth_vm,repo_list_vm,tasks_board_vm,members_vm,commits_vm,discord_messages_vm,daily_report_vm,stats_vm}.dart` (8)
- `lib/router/app_router.dart`
- `lib/views/{sign_in/sign_in_page,repos/repo_list_page,repos/add_repo_page,shell/repo_shell,tasks/tasks_board_page,tasks/add_todo_page,tasks/task_details_page,daily/daily_view_page,stats/stats_view_page,settings/settings_page,notify/notify_screen}.dart` (11)
- `functions/{package.json,tsconfig.json,.gitignore,.secret.local.example,README.md}`
- `functions/src/{index,admin,config,types}.ts`
- `functions/src/handlers/*.ts` (12)
- `functions/src/triggers/*.ts` (7)
- `functions/src/flows/*.ts` (4)
- `functions/src/prompts/*.ts` (4)
- `functions/src/tools/*.ts` (5)
- `functions/src/services/githubClient.ts`
- `secrets/{README.md,.gitkeep,openai.env.example,discord.env.example,github.env.example}`
- `firestore.rules`、`firestore.indexes.json`、`firebase.json`
- `pubspec.yaml`（mod：加入所有 deps）、`analysis_options.yaml`（mod：關 prefer_initializing_formals）、`.gitignore`（mod：加 secrets/ 與 functions/ 排除）
- `test/widget_test.dart`（mod：原本 import 已不存在的 `MyApp`，換成最小 smoke test）

**驗證了什麼 / 還沒驗證**

- ✅ `flutter pub get` 成功（29 套件可升但 SDK 仍能解）
- ✅ `npm install --prefix functions` 成功
- ✅ `flutter analyze` — **0 error / 0 warning**（只剩 1 個 info-level）
- ✅ `npm run typecheck` (TypeScript `tsc --noEmit`) — **0 error**
- ❌ `flutter run` 還沒跑（缺 `flutterfire configure`，會在 Firebase init 時 throw）
- ❌ `firebase emulators:start` 還沒跑（缺 `.secret.local`，但 emulator 本身應能啟動，functions 只在被呼叫時才會缺 secret）
- ❌ 各 view 的真實 UI 沒測（都是 Scaffold + ListView stub）

---

## 2026-05-26 — Journal 初始化

**做了什麼**
- 建立個人日誌檔。

**為何這樣做**
- 跟 AI_AGENT_RULES.md 規範一致。

**做不下去的地方 / 給下一個人**
- 還沒開始寫 code，待分配模組。

**動了哪些檔案**
- `docs/journal/113062210_chiajun.md` (new)

**驗證了什麼 / 還沒驗證**
- N/A
